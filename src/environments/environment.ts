// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebaseConfig : {
        apiKey: "AIzaSyAvKXR3H_EoP3l9ZaZaOzAYvueAFlr6U8A",
        authDomain: "gestion-usuarios-9f9f7.firebaseapp.com",
        projectId: "gestion-usuarios-9f9f7",
        storageBucket: "gestion-usuarios-9f9f7.appspot.com",
        messagingSenderId: "388518289489",
        appId: "1:388518289489:web:531c77bbbdb591bdaf1870"
      }
  };
  
  /*
   * For easier debugging in development mode, you can import the following file
   * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
   *
   * This import should be commented out in production mode because it will have a negative impact
   * on performance if an error is thrown.
   */
  // import 'zone.js/dist/zone-error';  // Included with Angular CLI.